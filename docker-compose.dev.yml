version: "3.9"

services:
  backend:
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - APP_ENV=development
    volumes:
      - ./backend/src:/app/src
